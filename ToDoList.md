# TODOs

## To finalize the program once all requirements are complete:
- Ensure help function is comprehensive
- Remove debugging print functions
- Clean up error handling for likely errors (i.e. passing in a negative offset (mapb for instance))
- Review API struct - one catchall or one per category of data